{
  "name": "uep-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix .. run prepublishOnly",
    "dev": "vite",
    "prebuild": "npm --prefix .. run prepublishOnly",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "universal-embed-player": "file:.."
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.2",
    "vite": "^6.3.5"
  }
}
